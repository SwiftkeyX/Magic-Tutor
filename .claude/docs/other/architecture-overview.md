> AUTO-GENERATED by /read-architecture — do not edit manually. Re-run to refresh.
> Last generated: 2026-07-12

---

## ActiveSkillSystem
**SRP**: The Active Skill System introduces mana, mana generation rules, and active spellcasting to all 30 heroes in the auto-battler.

*(No "Interactions with Other Systems" table in this GDD — derived from its Dependencies table.)*

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| AutoBattleResolver | Direct method call | High | Integrates casting states, mana loops, and skill resolution formulas directly into the resolver |
| HexGrid | Property read | Low | Reads hex coordinates/neighbors to evaluate targeting paths and clusters |
| BattleHUD | Property read | Low | Renders mana bars and skill cast info |

---

## AudioSystem
**SRP**: AudioSystem is the singleton audio manager that handles all music and SFX playback for the game.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | Property read + C# event | Low | Reads `CurrentState`; listens to `OnStateChanged` |
| RunManager | C# event | Low | Subscribes to `OnPhaseChanged` / `OnRunEnded` |
| TraitSystem | C# event | Low | Subscribes to `OnTraitThresholdReached` |
| AutoBattleResolver | C# event | Low | Subscribes to `OnBattleComplete` |
| PromotionSystem | C# event | Low | Subscribes to `OnPromotionComplete` |
| StudentRoster | C# event | Low | Subscribes to `OnRosterChanged` |

---

## AutoBattleResolver
**SRP**: AutoBattleResolver runs a tick-based combat simulation between the placed champion team and the enemy squad.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| BattleBoardManager | Direct method call + C# event | High | Calls `SetUnitPositions()` / `ApplyPreBattleTraitModifiers()` / `BeginBattle()`; subscribes to visual events |
| TraitEffectApplier | Direct method call | High | Calls `ApplyPreBattleTraitModifiers()` |
| ChampionRoster | Direct method call | High | Calls `GetStudents()` (lazy-init path) |
| EnemyDatabaseStub | Property read | Low | Provides enemy data (stub, pre-Phase-2) |
| BattleHUD | C# event | Low | Subscribes to `OnCombatantActed` / `OnCombatantDefeated` / `OnBattleComplete` |
| HexGrid | Direct method call | High | Queries `GetInRange`, occupancy, BFS pathing |

---

## BattleBoardManager
**SRP**: BattleBoardManager owns the hex grid visual layer in the Battle scene.

*(No "Interactions with Other Systems" table in this GDD — derived from its Dependencies table.)*

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| AutoBattleResolver | Direct method call + C# event | High | Reads snapshots; calls `SetUnitPositions()`; subscribes to movement/action/defeat/complete events |
| HexGrid | Direct method call | High | Grid adjacency, distance, BFS pathing |
| ChampionRoster | Property read | Low | Reads `ChampionData` for trait bookkeeping only |
| TraitTracker | Direct method call | High | `RegisterPlacement()` / `UnregisterPlacement()` |
| HeroSelection (static event) | Direct method call | High | `OnCardClicked()` resolves champion slug and calls `HeroSelection.Select()` |
| RunManager | Direct method call | High | Production only — calls `BeginPlacement()`; receives `ConfirmSquadPlacement()` callback |

---

## BattleHUD
**SRP**: BattleHUD renders the Battle scene's outcome UI.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| RunManager | Direct method call | High | "Continue" button calls `RunManager.CompleteBattlePhase()` |
| AutoBattleResolver | C# event + Property read | Low | Subscribes to `OnBattleComplete` only; reads `BattleResult` |
| InputHandler | C# event | Low | Subscribes to `OnSpeedUpStarted` / `OnSpeedUpCancelled` |
| ChampionRoster (via HeroInfoPanel) | Property read | Low | Reads `ChampionData` via `GetChampionById()` |
| HeroSelection (static event, via BenchCardDrag/BattleUnit) | C# event | Low | Decoupling utility — `OnHeroSelected` |
| BattleBoardManager | (none) | — | Explicitly decoupled peer — no reference in either direction; both subscribe to `AutoBattleResolver` independently |

---

## EnemyDatabase
**SRP**: EnemyDatabase is a ScriptableObject that stores all enemy definitions for the game.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| AutoBattleResolver | Direct method call | High | Calls `GetEnemiesForYear(year)` |

---

## GameManager
**SRP**: GameManager is the global singleton that owns the `GameState` enum and the active run snapshot.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| SceneLoader | Property read + Direct method call | High | Reads `CurrentState`; calls `LoadScene()` inside transitions |
| RunManager | Direct method call + Property read | High | Calls `EndRun()`; reads `TotalRunsStarted` |
| AudioSystem | C# event | Low | Subscribes to `OnGameStateChanged` |
| MainMenuController | Direct method call | High | Calls `StartNewRun()` |
| MetaProgressionHUD | Property read | Low | Reads `LastRunResult` |

---

## InputHandler
**SRP**: InputHandler wraps the Unity Input System and exposes typed C# events for non-UI gameplay inputs.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| SceneLoader | Direct method call | High | Calls `DisableGameplayInput()` / `EnableGameplayInput()` |
| RunManager | C# event | Low | Subscribes to `OnCancelPressed` |
| AutoBattleResolver | C# event | Low | Subscribes to `OnSpeedUpStarted` / `OnSpeedUpCancelled` |
| MainMenuController | C# event | Low | Subscribes to `OnConfirmPressed` |
| YearEndHUD | C# event | Low | Subscribes to `OnConfirmPressed` |

---

## MainMenuController
**SRP**: MainMenuController handles the main menu screen: new run, settings, and quit.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | Property read + Direct method call | High | Reads `CurrentState`; calls `StartNewRun()` |
| AudioSystem | Direct method call | High | Calls `SetMusicVolume()` / `SetSfxVolume()` |
| TeacherRoster | Property read | Low | Reads `GetAll()` for teacher count |

---

## MetaProgressionHUD
**SRP**: MetaProgressionHUD displays the player's accumulated teacher collection and run history between runs.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| TeacherRoster | Direct method call | High | Calls `GetAll()` / `GetTrainingBuff()` / `GetTeacherCountByFocus()` |
| SaveSystem | Direct method call | High | Calls `GetSaveData()` |
| MainMenuController | Direct method call | High | Opened/closed via MainMenuController's button |

---

## PromotionSystem
**SRP**: PromotionSystem drives the end-of-year transition: it presents surviving students as promotion candidates, lets the player choose which ones to elevate to teachers, converts selected students into `TeacherData` objects and hands them to TeacherRoster, removes all students from StudentRoster, then fires `OnPromotionComplete` for RunManager to advance the run.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| RunManager | C# event | Low | Fires `OnPromotionComplete` |
| StudentRoster | Direct method call | High | Calls `RemoveStudent()` / `Clear()` |
| TeacherRoster | Direct method call | High | Calls `AddTeacher()` |
| YearEndHUD | Direct method call + C# event | High | Exposes `GetCandidates()` / `ToggleSelection()` / `ConfirmPromotions()` |

---

## RunManager
**SRP**: RunManager drives the semester loop: Recruit → Train → Battle → YearEnd, repeated across 3 years.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | Property read + Direct method call | High | Reads `TotalRunsStarted`; calls `EndRun()` |
| SceneLoader | Direct method call | High | Calls `LoadScene()` at phase boundaries |
| StudentRoster | C# event | Low | `OnPhaseChanged` triggers `GenerateStudents()` |
| TraitSystem | Direct method call | High | Calls `ResolveBattleBuffs()` |
| AutoBattleResolver | Direct method call + C# event | High | Calls `Resolve()`; subscribes to `OnBattleComplete` |
| BattleBoardManager | Direct method call | High | Calls `BeginPlacement()`; receives `ConfirmSquadPlacement()` callback |
| BattleHUD | Direct method call | High | "Continue" button calls `CompleteBattlePhase()` |
| PromotionSystem | C# event | Low | Subscribes to `OnPromotionComplete` |
| EnemyDatabase | C# event | Low | `OnYearChanged` scales enemies for new year |
| InputHandler | C# event | Low | Subscribes to `OnCancelPressed` |

---

## SaveSystem
**SRP**: SaveSystem owns serialization and deserialization of all persistent cross-run data to disk.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| TeacherRoster | Direct method call | High | Calls `Save()` after every teacher mutation |

---

## SceneLoader
**SRP**: SceneLoader is the singleton that owns all scene transitions.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | Direct method call | High | Calls `LoadScene()` inside state transition methods |
| RunManager | Direct method call | High | Calls `LoadScene()` at phase boundaries |
| AudioSystem | C# event | Low | Subscribes to `OnSceneChanged` |

---

## SchoolHUD
**SRP**: SchoolHUD renders the School scene UI for the Recruit and Train phases.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| RunManager | Property read + C# event | Low | Reads `CurrentPhase`/`CurrentYear`; subscribes to `OnPhaseChanged`/`OnYearChanged` |
| StudentRoster | C# event | Low | Subscribes to `OnRosterChanged` / `OnStudentStatChanged` |
| TrainingSystem | Direct method call + Property read + C# event | High | Forwards `AllocateTraining()`; reads `RemainingActions`; subscribes to events |

---

## StudentRoster
**SRP**: StudentRoster manages the active list of students for the current semester.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| RunManager | C# event + Direct method call | High | `OnPhaseChanged` triggers generation; calls `Clear()` |
| TrainingSystem | Direct method call | High | Calls `NotifyStatChanged()` after writing bonus stats |
| TraitSystem | C# event | Low | Subscribes to `OnRosterChanged` |
| AutoBattleResolver | Property read | Low | Reads `GetAll()` during simulation |
| PromotionSystem | Direct method call | High | Calls `RemoveStudent()` |
| SchoolHUD | C# event | Low | Subscribes to `OnRosterChanged` / `OnStudentStatChanged` |

---

## TeacherRoster
**SRP**: TeacherRoster is the game's meta-progression store.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| SaveSystem | Direct method call | High | Calls `Load()` in `Awake()`, `Save()` in `AddTeacher()` |
| PromotionSystem | Direct method call | High | `AddTeacher()` is called into by PromotionSystem |
| TrainingSystem | Direct method call | High | `GetTrainingBuff()` is called into by TrainingSystem |
| MetaProgressionHUD | Direct method call | High | `GetAll()` / `GetTeacherCountByFocus()` called into by HUD |

---

## TrainingSystem
**SRP**: TrainingSystem implements the core player action: allocating a limited pool of training sessions to students to raise their stats.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| RunManager | Direct method call | High | Calls `InitializeSemester()` |
| StudentRoster | Direct method call | High | Calls `NotifyStatChanged()` |
| TeacherRoster | Property read | Low | Reads `GetTrainingBuff(statType)` |
| SchoolHUD | Direct method call + C# event | High | `AllocateTraining()` called by HUD; fires training events |

---

## TraitSystem
**SRP**: TraitSystem counts how many champions on the active battle board share each trait, determines which breakpoints are active, and applies their effects in two passes: (1) `TraitEffectApplier.Apply()` modifies combatant stats directly on `AutoBattleResolver` before the simulation begins, and (2) new per-unit variables on the internal `Combatant` class govern runtime combat behaviors during the tick loop.

*(No structured Interactions/Dependencies table in this GDD — derived from Quick Reference "Key deps" and Architecture section.)*

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| AutoBattleResolver | Direct method call | High | `TraitEffectApplier.Apply()` writes combatant stat mods pre-battle |
| BattleBoardManager | Direct method call | High | Registers/unregisters placements via `TraitTracker` |

---

## YearEndHUD
**SRP**: YearEndHUD renders the promotion/discard screen at the end of each year.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| PromotionSystem | Direct method call + C# event | High | Calls `GetCandidates()`/`ConfirmPromotions()`; subscribes to `OnPromotionComplete` |
